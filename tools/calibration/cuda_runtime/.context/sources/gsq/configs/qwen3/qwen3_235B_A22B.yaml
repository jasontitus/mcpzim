# Production config — Qwen3-235B-A22B (MoE)
# Uses paths relative to the working directory; set GSQ_RUNTIME in .env/shell if artifacts should leave the repo (see scripts/_common.sh).
#
# Model: Qwen/Qwen3-235B-A22B
#   - 128 routed experts, 8 active per token
#   - 94 decoder layers, decoder_sparse_step=1 (all layers are MoE)
#   - moe_intermediate_size=1536, hidden_size=4096
#
# Scaling guidance (4 GPUs/node):
#   2 nodes ( 8 GPUs) — 16 experts/GPU, ~24h for 94 layers
#   4 nodes (16 GPUs) —  8 experts/GPU, ~12h for 94 layers
#   8 nodes (32 GPUs) —  4 experts/GPU, ~6h  for 94 layers
#
# Scratch layout:
#   With GSQ_RUNTIME set, YAML may use paths like "${GSQ_RUNTIME}/checkpoints/...", "${GSQ_RUNTIME}/logs/...", etc. (expandvars in load_config).
#   Typical layout under <repo>/runtime (default GSQ_RUNTIME)
#     .hf/                         ← HF_HOME  (set in gsq.toml)
#     checkpoints/qwen3-235b/    ← per-layer .safetensors shards (active run)
#     logs/qwen3-235b/           ← Python training logs

model:
  name: "Qwen/Qwen3-235B-A22B"
  device: "cuda"
  dtype: "bfloat16"

data:
  dataset_name: "open_thoughts"
  batch_size: 64
  num_samples: 4096
  max_length: 4096
  num_workers: 8
  val_samples: 128

quantization:
  gsq_bits: 2
  init_method: "gptq"
  gsq_enabled: true
  start_layer: 0
  self_attn: false
  std: 0.01
  temperature: [2, 0.05]
  scale: [100, 500]
  groupsize: 128
  strength: 6
  logits_dtype: "bfloat16"

training:
  num_epochs: 10
  device_microbatch_size: 2
  masks_lr: 0.0002
  signs_lr: 0.0001
  scales_lr: 0.0001
  weight_decay: 1.0
  checkpoint_dir: "checkpoints/qwen3-235b"
  log_dir: "logs/qwen3-235b"
  act_cache_dir: "cache/act_cache"
  eval_baseline: true
  # ppl_eval_every_n_layers: 6

gptq:
  nsamples: 512
  wbits: 2
  sym: true
  trits: false
  percdamp: 0.1
  blocksize: 128
  groupsize: 128
  static_groups: false
  prunen: 0
  prunem: 0

wandb:
  enabled: true
  entity: "ist"
  project: "GSQ-multi"
